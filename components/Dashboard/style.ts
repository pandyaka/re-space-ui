import styled from 'styled-components';
import { Title, H2Style, Small } from '@components/Typography';

export const DashboardStyle = styled.div`
    width: 100%;
    margin: 48px 0;
`;

export const DashboardCaption = styled(Title)``;

export const SectionCaption = styled(H2Style)`
    margin-top: 72px;
`;

export const SectionDescription = styled(Small)``;
